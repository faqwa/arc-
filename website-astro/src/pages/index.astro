---
import BaseLayout from '../layouts/BaseLayout.astro';
import ProjectCard from '../components/ProjectCard.jsx';
import projects from '../data/projects.json';
---
<BaseLayout
  title="Arc^ Initiative — Because we can & because we must"
  description="Arc^ Initiative: a Gen Z-led community using plasma-activated water to heal ecosystems."
>
  <section
    id="discover"
    class="relative flex min-h-screen items-center justify-center overflow-hidden px-6 text-center"
  >
    <!-- TODO: Replace placeholder gradients with video or generative plasma art -->
    <div class="manifesto-bg manifesto-bg--erosion"></div>
    <div class="relative z-10 mx-auto max-w-3xl space-y-6">
      <p class="text-sm uppercase tracking-[0.6em] text-white/60">field report</p>
      <h1 class="text-4xl font-semibold leading-tight md:text-6xl">
        the planet is bleeding out.
      </h1>
      <p class="text-lg text-white/80">
        coastlines collapsing. seed banks shrinking. we are the generation that gets to decide if this was a prologue or an obituary.
      </p>
    </div>
    <div class="scroll-indicator">
      <span>scroll</span>
      <span class="text-white">↓</span>
    </div>
  </section>

  <section class="relative flex min-h-screen items-center justify-center overflow-hidden px-6 text-center">
    <!-- TODO: Swap placeholder gradients with climate data visualization -->
    <div class="manifesto-bg manifesto-bg--data"></div>
    <div class="relative z-10 mx-auto max-w-2xl space-y-7">
      <p class="text-2xl font-semibold text-white">
        1.5° C is not a plateau.
      </p>
      <p class="text-lg text-white/80">
        arctic sea ice <span class="font-semibold text-white">↓ 13% per decade</span>.<br />
        freshwater access projected to displace <span class="font-semibold text-white">700 million people</span>.<br />
        plasma can detoxify water faster than regulation can keep up.
      </p>
      <p class="text-sm uppercase tracking-[0.5em] text-white/60">
        imagine this screen pulsing with real-time stats.
      </p>
    </div>
  </section>

  <section class="relative flex min-h-screen items-center justify-center overflow-hidden px-6 text-center">
    <!-- TODO: Replace with footage of prototypes / community labs -->
    <div class="manifesto-bg manifesto-bg--call"></div>
    <div class="relative z-10 mx-auto max-w-2xl space-y-8">
      <h2 class="text-3xl font-semibold md:text-5xl">we built arc^ so students could build futures.</h2>
      <p class="text-lg text-white/80">
        plasma-activated water is the tool. reciprocity is the rule. bring your lab, your honours project, your midnight questions.
      </p>
      <a
        href="#main-content"
        class="inline-flex items-center justify-center rounded-full bg-white/90 px-8 py-3 text-sm font-semibold uppercase tracking-[0.4em] text-black transition hover:bg-white"
      >
        enter arc^
      </a>
    </div>
  </section>

  <div id="main-content" class="bg-transparent">
    <section class="mx-auto max-w-5xl px-6 pb-20 pt-16">
      <div class="grid gap-6 md:grid-cols-3">
        {[
          {
            title: 'who we are',
            body:
              'a collective of engineers, scientists, designers, and systems thinkers based in australia, building a global commons around plasma-activated water.',
          },
          {
            title: 'why we share',
            body:
              'open licences keep plasma tech from being enclosed. everything we publish demands reciprocity—use it, iterate, publish back.',
          },
          {
            title: 'how we work',
            body:
              'we start with narratives. once you align with our principles, you dive into protocols, cad, datasets, and transparent taskboards.',
          },
        ].map((card) => (
          <div class="rounded-3xl border border-white/10 bg-white/5 p-6 text-left" key={card.title}>
            <h3 class="text-xl font-semibold text-white">{card.title}</h3>
            <p class="mt-4 text-sm text-muted">{card.body}</p>
          </div>
        ))}
      </div>
    </section>

    <section class="mx-auto max-w-6xl px-6 pb-20">
      <div class="mb-10 flex flex-col gap-3 md:flex-row md:items-end md:justify-between">
        <div>
          <p class="text-sm uppercase tracking-[0.4em] text-muted">project library</p>
          <h2 class="text-3xl font-semibold">active & emerging initiatives</h2>
        </div>
        <a href="/projects" class="text-sm font-semibold text-muted underline decoration-dotted underline-offset-4">
          view all projects
        </a>
      </div>
      <div class="grid grid-cols-1 gap-6 md:grid-cols-2">
        {projects.map((p) => <ProjectCard key={p.slug} {...p} />)}
      </div>
      <div class="mt-8 rounded-2xl border border-white/10 bg-white/5 p-6 text-sm text-muted">
        <strong class="font-semibold text-white">transparency note:</strong> task lists, open questions, data requests—published in the open so you can self-assign.
      </div>
    </section>

    <section class="mx-auto max-w-5xl px-6 pb-20">
      <div class="grid gap-6 md:grid-cols-2">
        <div class="rounded-3xl border border-[#ffb703]/40 bg-[#ffb703]/10 p-8 text-sm text-[#ffd166]">
          <p class="text-xs uppercase tracking-[0.4em]">safety first</p>
          <h2 class="mt-3 text-2xl font-semibold text-white">plasma is powerful. treat it with respect.</h2>
          <p class="mt-4 text-base text-[#ffeec7]">
            accessible warnings for curious builders. formal risk assessments for labs. you are responsible for matching your jurisdiction’s rules.
          </p>
          <a href="/safety" class="mt-6 inline-block text-sm font-semibold text-white underline underline-offset-4">
            read the safety primer
          </a>
          <p class="mt-2 text-xs uppercase tracking-[0.4em]">templates in development</p>
        </div>
        <div class="rounded-3xl border border-white/10 bg-white/5 p-8 text-sm text-muted">
          <h2 class="text-lg font-semibold text-white">what to expect</h2>
          <ul class="mt-4 space-y-3">
            <li>download narratives + arc^ laws to prep your supervisor pitch.</li>
            <li>enter project-specific channels (discord placeholder) when onboarding opens.</li>
            <li>log progress in public dashboards so other teams can sync.</li>
          </ul>
        </div>
      </div>
    </section>

    <section class="mx-auto max-w-5xl px-6 pb-24 text-center">
      <h2 class="text-3xl font-semibold">ready to orbit arc^ with your thesis or lab?</h2>
      <p class="mt-4 text-lg text-muted">
        download the starter pack (placeholder), map out your research path, then let us know how you want to contribute.
      </p>
      <div class="mt-8 flex flex-wrap justify-center gap-4" id="actions">
        <a
          href="/join"
          class="rounded-full bg-arkup-teal/90 px-6 py-3 text-sm font-semibold uppercase tracking-[0.4em] text-white shadow-lg shadow-arkup-teal/20 transition hover:bg-arkup-teal"
        >
          explore join options
        </a>
        <a
          href="mailto:writingsbyali@gmail.com"
          class="rounded-full border border-white/30 px-6 py-3 text-sm font-semibold uppercase tracking-[0.4em] text-white transition hover:border-white hover:bg-white/5"
        >
          email the initiative
        </a>
      </div>
    </section>
  </div>
</BaseLayout>
