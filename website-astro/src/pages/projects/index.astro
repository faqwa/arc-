---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProjectCard from '../../components/ProjectCard.jsx';
import projects from '../../data/projects.json';
---
<BaseLayout title="Arc^ Projects" description="Explore Arc^ Initiative project concepts and see where your research can plug in.">
  <section class="mx-auto max-w-5xl px-6 py-20">
    <p class="text-sm uppercase tracking-[0.4em] text-muted">Project index</p>
    <h1 class="mt-3 text-4xl font-semibold">Choose the work that resonates</h1>
    <p class="mt-6 text-lg text-muted">
      Every Arc^ project starts with a question. We publish the hypotheses, safety expectations, and open tasks so you can build your own study plan. Select a project to see the narrative, emerging protocols, and transparency logs.
    </p>
    <div class="mt-12 grid grid-cols-1 gap-6 md:grid-cols-2">
      {projects.map((p) => <ProjectCard key={p.slug} {...p} />)}
    </div>
  </section>

  <section class="mx-auto max-w-5xl px-6 pb-24">
    <div class="rounded-3xl border border-white/10 bg-white/5 p-6 text-sm text-muted">
      <h2 class="text-xl font-semibold text-white">Project lifecycle</h2>
      <ol class="mt-4 list-decimal space-y-2 pl-6">
        <li>Publish concept brief and licensing expectations.</li>
        <li>Release safety primer + risk assessment draft.</li>
        <li>Prototype hardware/recipes and invite replication partners.</li>
        <li>Pool datasets, analyse together, and iterate transparently.</li>
      </ol>
    </div>
  </section>
</BaseLayout>
